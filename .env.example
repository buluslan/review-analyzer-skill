# ==================== 数据源配置 ====================

# 主源为本地 CSV（从任何平台导出评论，覆盖全、正文完整，零配置）
# 下面为可选增强源：卖家精灵（输入 ASIN 快速预览，覆盖量有限、部分正文可能缺失）

# 卖家精灵 secret-key (可选，用于从卖家精灵获取评论)
# 获取地址: https://www.sellersprite.com/open/api
SELLERSPRITE_SECRET_KEY=your_sellersprite_secret_key_here

# ==================== 分析配置 ====================

# 最大获取评论数 (默认: 500)
MAX_REVIEWS=500

# 每批处理数量 (默认: 30, 范围: 20-50)
# 注意: main.py命令行参数默认为20,此处为config.py中的默认值
BATCH_SIZE=30

# ==================== 输出配置 ====================

# 是否生成 HTML 报告 (默认: true)
GENERATE_HTML=true

# 报告署名 (默认: Buluu@新西楼)
HTML_CREATOR_NAME=Buluu@新西楼

# ==================== CLI 引擎配置 ====================

# CLI 引擎选择 (默认: 自动探测)
# - claude: 使用 Claude Code CLI (claude --print)
# - opencode: 使用 OpenCode CLI (opencode run)
# - 留空或不设置: 自动探测（优先 claude，其次 opencode）
CLI_ENGINE=claude

# CLI 调用超时时间，单位秒 (默认: 600)
CLI_TIMEOUT=600

# ==================== 高级配置 ====================

# 最大并发子代理数 (默认: 4, 上限: 8)
# 注意: 过高可能导致CLI超时,建议范围 2-6
MAX_CONCURRENT=4

# 最多识别的用户画像数 (默认: 4)
MAX_PERSONAS=4

# 每个画像的样本数 (默认: 6, 即 3正+3负)
SAMPLES_PER_PERSONA=6
